1. Input: 
          input(),
"The function pauses execution and waits for user text. By not passing an argument, the default non-explicit prompt is used."
2. Lowercase:
         .lower()
This built-in string method is chained directly after input(). It converts all uppercase letters to lowercase while correctly preserving punctuation and whitespace (as required by the task).
3. Output:
         print()
"Displays the final, modified string to the console."


